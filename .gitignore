# 1. Ignore everything by default to prevent open sourcing your extension
/*
!/.gitignore

# 2. Add back ONLY the paths absolutely needed for GitHub Actions
!/.github/
!/.github/workflows/
!/.github/workflows/daily-scraper.yml

!/backend/
/backend/*
!/backend/scraper/
!/backend/scraper/scraper.js
!/backend/scraper/package.json
!/backend/scraper/package-lock.json
!/backend/scraper/scraper_state.json

!/scrape_grabon.py

# 3. CRITICAL: Never accidentally sync secret keys or large node modules!
.env
node_modules/
