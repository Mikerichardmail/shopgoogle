<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- ===== BOTTOM NAVIGATION ===== -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#FFFFFF"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:menu="@menu/bottom_nav_menu"
        android:elevation="8dp"/>

    <!-- ===== SCROLLABLE CONTENT ===== -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottomNavigation">
        
        <include layout="@layout/fragment_categories" android:id="@+id/containerCategories" android:visibility="gone" />
        <include layout="@layout/fragment_stores" android:id="@+id/containerStores" android:visibility="gone" />
        <include layout="@layout/fragment_coupons" android:id="@+id/containerCoupons" android:visibility="gone" />
        <include layout="@layout/fragment_settings" android:id="@+id/containerSettings" android:visibility="gone" />
        
    </FrameLayout>

</RelativeLayout>